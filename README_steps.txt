Pasos finales para activar la subida automática:
1) Abre Git Bash en esta carpeta:
   cd "/mnt/data/Form.Recep_project/Form.Recep"
2) Da permisos al hook (solo primera vez):
   chmod +x .git/hooks/post-commit
3) A partir de ahora: Commit en GitHub Desktop => push automático => GitHub Pages publica solo.
